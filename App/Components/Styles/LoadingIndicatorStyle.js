import {StyleSheet} from 'react-native'
export default StyleSheet.create({
    activityIndicator: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        height: 100,
        padding:10,
     },
});
